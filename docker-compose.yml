services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: casefinder
      POSTGRES_PASSWORD: casefinder
      POSTGRES_DB: casefinder
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
